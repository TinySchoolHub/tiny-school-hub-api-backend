apiVersion: v1
kind: Service
metadata:
  name: {{ include "tiny-school-hub.fullname" . }}
  labels:
    {{- include "tiny-school-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tiny-school-hub.selectorLabels" . | nindent 4 }}
